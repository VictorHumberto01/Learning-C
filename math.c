#include <stdio.h>
#include <math.h>

int main() {

    double A = sqrt(9);
    double B = pow(2, 4); //2^3 (2 to the power of 3)
    int C = round(3.14);
    int D = ceil(3.14); //round up
    int E = floor(3.99); //round down
    double F = fabs(-100.0); //absolute value
    double G = log(3);
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);

    printf("\n%lf\n", F);

    return 0;
}